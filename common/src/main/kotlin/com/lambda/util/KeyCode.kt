package com.lambda.util

import org.lwjgl.glfw.GLFW

@JvmInline
value class KeyCode(val key: Int) {
    companion object {
        val Unbound = KeyCode(GLFW.GLFW_KEY_UNKNOWN)
        val Space = KeyCode(GLFW.GLFW_KEY_SPACE)
        val Apostrophe = KeyCode(GLFW.GLFW_KEY_APOSTROPHE)
        val Comma = KeyCode(GLFW.GLFW_KEY_COMMA)
        val Minus = KeyCode(GLFW.GLFW_KEY_MINUS)
        val Period = KeyCode(GLFW.GLFW_KEY_PERIOD)
        val Slash = KeyCode(GLFW.GLFW_KEY_SLASH)
        val Num0 = KeyCode(GLFW.GLFW_KEY_0)
        val Num1 = KeyCode(GLFW.GLFW_KEY_1)
        val Num2 = KeyCode(GLFW.GLFW_KEY_2)
        val Num3 = KeyCode(GLFW.GLFW_KEY_3)
        val Num4 = KeyCode(GLFW.GLFW_KEY_4)
        val Num5 = KeyCode(GLFW.GLFW_KEY_5)
        val Num6 = KeyCode(GLFW.GLFW_KEY_6)
        val Num7 = KeyCode(GLFW.GLFW_KEY_7)
        val Num8 = KeyCode(GLFW.GLFW_KEY_8)
        val Num9 = KeyCode(GLFW.GLFW_KEY_9)
        val Semicolon = KeyCode(GLFW.GLFW_KEY_SEMICOLON)
        val Equal = KeyCode(GLFW.GLFW_KEY_EQUAL)
        val A = KeyCode(GLFW.GLFW_KEY_A)
        val B = KeyCode(GLFW.GLFW_KEY_B)
        val C = KeyCode(GLFW.GLFW_KEY_C)
        val D = KeyCode(GLFW.GLFW_KEY_D)
        val E = KeyCode(GLFW.GLFW_KEY_E)
        val F = KeyCode(GLFW.GLFW_KEY_F)
        val G = KeyCode(GLFW.GLFW_KEY_G)
        val H = KeyCode(GLFW.GLFW_KEY_H)
        val I = KeyCode(GLFW.GLFW_KEY_I)
        val J = KeyCode(GLFW.GLFW_KEY_J)
        val K = KeyCode(GLFW.GLFW_KEY_K)
        val L = KeyCode(GLFW.GLFW_KEY_L)
        val M = KeyCode(GLFW.GLFW_KEY_M)
        val N = KeyCode(GLFW.GLFW_KEY_N)
        val O = KeyCode(GLFW.GLFW_KEY_O)
        val P = KeyCode(GLFW.GLFW_KEY_P)
        val Q = KeyCode(GLFW.GLFW_KEY_Q)
        val R = KeyCode(GLFW.GLFW_KEY_R)
        val S = KeyCode(GLFW.GLFW_KEY_S)
        val T = KeyCode(GLFW.GLFW_KEY_T)
        val U = KeyCode(GLFW.GLFW_KEY_U)
        val V = KeyCode(GLFW.GLFW_KEY_V)
        val W = KeyCode(GLFW.GLFW_KEY_W)
        val X = KeyCode(GLFW.GLFW_KEY_X)
        val Y = KeyCode(GLFW.GLFW_KEY_Y)
        val Z = KeyCode(GLFW.GLFW_KEY_Z)
        val LeftBracket = KeyCode(GLFW.GLFW_KEY_LEFT_BRACKET)
        val Backslash = KeyCode(GLFW.GLFW_KEY_BACKSLASH)
        val RightBracket = KeyCode(GLFW.GLFW_KEY_RIGHT_BRACKET)
        val GraveAccent = KeyCode(GLFW.GLFW_KEY_GRAVE_ACCENT)
        val World1 = KeyCode(GLFW.GLFW_KEY_WORLD_1)
        val World2 = KeyCode(GLFW.GLFW_KEY_WORLD_2)
        val Escape = KeyCode(GLFW.GLFW_KEY_ESCAPE)
        val Enter = KeyCode(GLFW.GLFW_KEY_ENTER)
        val Tab = KeyCode(GLFW.GLFW_KEY_TAB)
        val Backspace = KeyCode(GLFW.GLFW_KEY_BACKSPACE)
        val Insert = KeyCode(GLFW.GLFW_KEY_INSERT)
        val Delete = KeyCode(GLFW.GLFW_KEY_DELETE)
        val Right = KeyCode(GLFW.GLFW_KEY_RIGHT)
        val Left = KeyCode(GLFW.GLFW_KEY_LEFT)
        val Down = KeyCode(GLFW.GLFW_KEY_DOWN)
        val Up = KeyCode(GLFW.GLFW_KEY_UP)
        val PageUp = KeyCode(GLFW.GLFW_KEY_PAGE_UP)
        val PageDown = KeyCode(GLFW.GLFW_KEY_PAGE_DOWN)
        val Home = KeyCode(GLFW.GLFW_KEY_HOME)
        val End = KeyCode(GLFW.GLFW_KEY_END)
        val CapsLock = KeyCode(GLFW.GLFW_KEY_CAPS_LOCK)
        val ScrollLock = KeyCode(GLFW.GLFW_KEY_SCROLL_LOCK)
        val NumLock = KeyCode(GLFW.GLFW_KEY_NUM_LOCK)
        val PrintScreen = KeyCode(GLFW.GLFW_KEY_PRINT_SCREEN)
        val Pause = KeyCode(GLFW.GLFW_KEY_PAUSE)
        val F1 = KeyCode(GLFW.GLFW_KEY_F1)
        val F2 = KeyCode(GLFW.GLFW_KEY_F2)
        val F3 = KeyCode(GLFW.GLFW_KEY_F3)
        val F4 = KeyCode(GLFW.GLFW_KEY_F4)
        val F5 = KeyCode(GLFW.GLFW_KEY_F5)
        val F6 = KeyCode(GLFW.GLFW_KEY_F6)
        val F7 = KeyCode(GLFW.GLFW_KEY_F7)
        val F8 = KeyCode(GLFW.GLFW_KEY_F8)
        val F9 = KeyCode(GLFW.GLFW_KEY_F9)
        val F10 = KeyCode(GLFW.GLFW_KEY_F10)
        val F11 = KeyCode(GLFW.GLFW_KEY_F11)
        val F12 = KeyCode(GLFW.GLFW_KEY_F12)
        val F13 = KeyCode(GLFW.GLFW_KEY_F13)
        val F14 = KeyCode(GLFW.GLFW_KEY_F14)
        val F15 = KeyCode(GLFW.GLFW_KEY_F15)
        val F16 = KeyCode(GLFW.GLFW_KEY_F16)
        val F17 = KeyCode(GLFW.GLFW_KEY_F17)
        val F18 = KeyCode(GLFW.GLFW_KEY_F18)
        val F19 = KeyCode(GLFW.GLFW_KEY_F19)
        val F20 = KeyCode(GLFW.GLFW_KEY_F20)
        val F21 = KeyCode(GLFW.GLFW_KEY_F21)
        val F22 = KeyCode(GLFW.GLFW_KEY_F22)
        val F23 = KeyCode(GLFW.GLFW_KEY_F23)
        val F24 = KeyCode(GLFW.GLFW_KEY_F24)
        val F25 = KeyCode(GLFW.GLFW_KEY_F25)
        val KP_0 = KeyCode(GLFW.GLFW_KEY_KP_0)
        val KP_1 = KeyCode(GLFW.GLFW_KEY_KP_1)
        val KP_2 = KeyCode(GLFW.GLFW_KEY_KP_2)
        val KP_3 = KeyCode(GLFW.GLFW_KEY_KP_3)
        val KP_4 = KeyCode(GLFW.GLFW_KEY_KP_4)
        val KP_5 = KeyCode(GLFW.GLFW_KEY_KP_5)
        val KP_6 = KeyCode(GLFW.GLFW_KEY_KP_6)
        val KP_7 = KeyCode(GLFW.GLFW_KEY_KP_7)
        val KP_8 = KeyCode(GLFW.GLFW_KEY_KP_8)
        val KP_9 = KeyCode(GLFW.GLFW_KEY_KP_9)
        val KP_DECIMAL = KeyCode(GLFW.GLFW_KEY_KP_DECIMAL)
        val KP_DIVIDE = KeyCode(GLFW.GLFW_KEY_KP_DIVIDE)
        val KP_MULTIPLY = KeyCode(GLFW.GLFW_KEY_KP_MULTIPLY)
        val KP_SUBTRACT = KeyCode(GLFW.GLFW_KEY_KP_SUBTRACT)
        val KP_ADD = KeyCode(GLFW.GLFW_KEY_KP_ADD)
        val KP_ENTER = KeyCode(GLFW.GLFW_KEY_KP_ENTER)
        val KP_EQUAL = KeyCode(GLFW.GLFW_KEY_KP_EQUAL)
        val LEFT_SHIFT = KeyCode(GLFW.GLFW_KEY_LEFT_SHIFT)
        val LEFT_CONTROL = KeyCode(GLFW.GLFW_KEY_LEFT_CONTROL)
        val LEFT_ALT = KeyCode(GLFW.GLFW_KEY_LEFT_ALT)
        val LEFT_SUPER = KeyCode(GLFW.GLFW_KEY_LEFT_SUPER)
        val RIGHT_SHIFT = KeyCode(GLFW.GLFW_KEY_RIGHT_SHIFT)
        val RIGHT_CONTROL = KeyCode(GLFW.GLFW_KEY_RIGHT_CONTROL)
        val RIGHT_ALT = KeyCode(GLFW.GLFW_KEY_RIGHT_ALT)
        val RIGHT_SUPER = KeyCode(GLFW.GLFW_KEY_RIGHT_SUPER)
        val MENU = KeyCode(GLFW.GLFW_KEY_MENU)
        val LAST = KeyCode(GLFW.GLFW_KEY_LAST)
    }
}