package com.lambda.config.configurations

import com.lambda.config.Configuration

/**
 * The [LambdaConfig] object represents the configuration file for the lambda.
 */
object LambdaConfig : Configuration(configName = "lambda")
